[package]
name = "dsh-launch-environment"
version = "0.1.0-rc.5"
edition = "2024"
license = "MIT"
description = "Immutable launch-time environment snapshot with per-layer provenance: Rust port of @deepseek-ai/dsh-launch-environment"

[dependencies]
cordis = { path = "../../vendor/cordis" }
dsh-invariants = { path = "../../util/invariants" }
async-trait = { workspace = true }
futures = { workspace = true }
