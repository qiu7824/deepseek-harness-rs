[package]
name = "dsh-fs"
version = "0.1.0-rc.8"
edition = "2024"
license = "MIT"
description = "Filesystem Service Definition (ctx.fs) for one execution world: Rust port of @deepseek-ai/dsh-fs"

[dependencies]
cordis = { path = "../../vendor/cordis" }
dsh-brand = { path = "../../util/brand" }
dsh-llm = { path = "../../core/llm" }
dsh-sandbox = { path = "../../sandbox/sandbox" }
dsh-invariants = { path = "../../util/invariants" }
async-trait = { workspace = true }
futures = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
parking_lot = { workspace = true }
