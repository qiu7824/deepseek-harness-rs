[package]
name = "dsh-subprocess"
version = "0.1.0-rc.8"
edition = "2024"
license = "MIT"
description = "Service Definition for the subprocess capability seam (ctx.subprocess): Rust port of @deepseek-ai/dsh-subprocess"

[dependencies]
cordis = { path = "../../vendor/cordis" }
dsh-invariants = { path = "../../util/invariants" }
regex = { workspace = true }
async-trait = { workspace = true }
tokio = { workspace = true }
futures = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
futures = { workspace = true }
parking_lot = { workspace = true }
