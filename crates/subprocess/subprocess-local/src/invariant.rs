//! Invariant hooks for the local subprocess backend. The TS package carries
//! none: every disposition and limit arrives on the spawn spec, and the
//! termination ladder is exercised through the seam's own tests.

// (intentionally empty)
