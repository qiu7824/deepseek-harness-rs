[package]
name = "dsh-agent-instructions"
version = "0.1.0-rc.8"
edition = "2024"
license = "MIT"
description = "Durable AGENTS.md-compatible workspace instruction context"

[dependencies]
cordis = { path = "../../vendor/cordis" }
dsh-agent = { path = "../../core/agent" }
dsh-llm = { path = "../../core/llm" }
dsh-session = { path = "../../core/session" }
futures = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
tempfile = "3"
