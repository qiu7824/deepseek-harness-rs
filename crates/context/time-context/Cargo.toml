[package]
name = "dsh-time-context"
version = "0.1.0-rc.5"
edition = "2024"
license = "MIT"
description = "Opt-in durable per-step context with the current time and elapsed time — Rust port of @deepseek-ai/dsh-time-context"

[dependencies]
cordis = { path = "../../vendor/cordis" }
dsh-agent = { path = "../../core/agent" }
dsh-invariants = { path = "../../util/invariants" }
dsh-llm = { path = "../../core/llm" }
dsh-schemastery = { path = "../../vendor/schemastery" }
dsh-session = { path = "../../core/session" }
anyhow = { workspace = true }
async-trait = { workspace = true }
chrono = { workspace = true }
iana-time-zone = "0.1"
jiff = "0.2"
indexmap = { workspace = true }
parking_lot = { workspace = true }
regex = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
dsh-scope = { path = "../../core/scope" }
async-trait = { workspace = true }
futures = { workspace = true }
tokio = { workspace = true }
