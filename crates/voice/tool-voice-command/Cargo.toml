[package]
name = "dsh-tool-voice-command"
version = "0.1.0-rc.8"
edition = "2024"
license = "MIT"
description = "External command adapters for optional STT and TTS tools"

[dependencies]
cordis = { path = "../../vendor/cordis" }
dsh-llm = { path = "../../core/llm" }
dsh-native-command = { path = "../../util/native-command" }
dsh-tools = { path = "../../core/tools" }
futures = { workspace = true }
serde_json = { workspace = true }
