[package]
name = "dsh-terminal-bash"
version = "0.1.0-rc.5"
edition = "2024"
license = "MIT"
description = "Persistent local shell PTY backend over the subprocess terminal seam"

[dependencies]
cordis = { path = "../../vendor/cordis" }
dsh-sandbox = { path = "../../sandbox/sandbox" }
dsh-sandbox-policy = { path = "../../sandbox/sandbox-policy" }
dsh-subprocess = { path = "../../subprocess/subprocess" }
dsh-terminal = { path = "../terminal" }
futures = { workspace = true }
parking_lot = { workspace = true }
tokio = { workspace = true }
