[package]
name = "dsh-spill"
version = "0.1.0-rc.8"
edition = "2024"
license = "MIT"
description = "Service Definition for the spill storage capability seam (ctx.spillStore): Rust port of @deepseek-ai/dsh-spill"

[dependencies]
cordis = { path = "../../vendor/cordis" }
dsh-brand = { path = "../../util/brand" }
dsh-llm = { path = "../../core/llm" }
dsh-session = { path = "../../core/session" }
dsh-invariants = { path = "../../util/invariants" }
async-trait = { workspace = true }
futures = { workspace = true }
