[package]
name = "dsh-tool-goal"
version = "0.1.0-rc.8"
edition = "2024"
license = "MIT"
description = "Model-facing same-session goal tools with execution-time authority checks — Rust port of @deepseek-ai/dsh-tool-goal"

[dependencies]
anyhow = { workspace = true }
async-trait = { workspace = true }
cordis = { path = "../../vendor/cordis" }
dsh-agent = { path = "../../core/agent" }
dsh-goal = { path = "../goal" }
dsh-invariants = { path = "../../util/invariants" }
dsh-llm = { path = "../../core/llm" }
dsh-session = { path = "../../core/session" }
dsh-scope = { path = "../../core/scope" }
dsh-system-prompt = { path = "../../core/system-prompt" }
dsh-tools = { path = "../../core/tools" }
serde_json = { workspace = true }

[dev-dependencies]
futures = { workspace = true }
tokio = { workspace = true }
