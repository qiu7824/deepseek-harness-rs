[package]
name = "dsh-compaction"
version = "0.1.0-rc.5"
edition = "2024"
license = "MIT"
description = "Abstract compaction service seam (ctx.compaction) — Rust port of @deepseek-ai/dsh-compaction"

[dependencies]
cordis = { path = "../../vendor/cordis" }
dsh-brand = { path = "../../util/brand" }
dsh-commands = { path = "../../interaction/commands" }
dsh-invariants = { path = "../../util/invariants" }
dsh-llm = { path = "../../core/llm" }
dsh-session = { path = "../../core/session" }
async-trait = { workspace = true }
anyhow = { workspace = true }
parking_lot = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
async-trait = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
