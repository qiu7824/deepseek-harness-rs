[package]
name = "dsh-storage-sqlite"
version = "0.1.0-rc.8"
edition = "2024"
license = "MIT"
description = "SQLite storage backend for the storage hub: one database file hosts every routed unit, document-per-row: Rust port of @deepseek-ai/dsh-storage-sqlite"

[dependencies]
cordis = { path = "../../vendor/cordis" }
dsh-storage = { path = "../storage" }
dsh-invariants = { path = "../../util/invariants" }
serde = { workspace = true }
serde_json = { workspace = true }
parking_lot = { workspace = true }
tokio = { workspace = true }
futures = { workspace = true }
async-trait = { workspace = true }
rusqlite = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
