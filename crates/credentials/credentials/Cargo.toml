[package]
name = "dsh-credentials"
version = "0.1.0-rc.8"
edition = "2024"
license = "MIT"
description = "Abstract credential seam (ctx.credentials): settings carry references to secrets, providers own the values: Rust port of @deepseek-ai/dsh-credentials"

[dependencies]
cordis = { path = "../../vendor/cordis" }
dsh-brand = { path = "../../util/brand" }
dsh-invariants = { path = "../../util/invariants" }
regex = { workspace = true }
async-trait = { workspace = true }
futures = { workspace = true }
parking_lot = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
