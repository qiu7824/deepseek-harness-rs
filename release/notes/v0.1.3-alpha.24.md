# 0.1.3-alpha.24

- 保留标准鲸鱼图标，Windows 桌面、开始菜单和卸载入口显式使用与托盘相同的图标；安装结束后通知 Shell 刷新缓存。
- 核验主程序、启动器及安装器的全部内嵌图标帧，防止发布资源与托盘图标不一致；非 Windows 启动器使用同图案的 PNG 资源。
- 修复账号登录弹窗中供应商切换按钮的样式、选中状态、禁用状态和键盘焦点；嵌入式登录操作复用标准按钮样式。
- 侧栏回归按查看器身份校验 Office 预览与旧格式下载，并验证所有注册项均被卸载；不再依赖过期数量和数组位置。
- 修正发布所需的 Rust 源码格式，保持已有功能和会话数据兼容。
