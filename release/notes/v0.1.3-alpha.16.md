# DeepSeek Harness Rust v0.1.3-alpha.16

- 压缩历史读取缓冲区按实际内容增长，不再按 65,536 条事件的扫描上限预分配整块内存。页面候选、压缩帧和打包文本三处缓冲区均采用小容量起步。
- 保留事件数量、来源关系和历史窗口的安全上限，不裁剪消息内容。
- 加入实际分配量回归测试：读取同样的 8 条事件时，提高扫描上限不应导致分配量大幅增加。

保留 [alpha.15](v0.1.3-alpha.15.md) 的分页预取与内置订阅授权功能。
