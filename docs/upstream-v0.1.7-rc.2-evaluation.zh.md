# DeepSeek Harness Rust：v0.1.7-rc.2 兼容评估

上游基线：[dsh-v0.1.7-rc.2](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.7-rc.2)，提交 `477b4f420553e8a52c2fbccc464d7561b239c443`。Rust 源码基线为 `0.1.3-alpha.35`，两者版本号独立维护。

## 会话历史兼容

升级到 Session V4 后，`session.list` 曾只保留当前格式版本，导致仍在磁盘上的 V3 会话从 Web 和 Flutter 列表消失。列表改为使用持久化层已经验证的可读会话，不再按当前写入版本过滤；未挂载投影缓存时也读取真实的空白状态和更新时间。

冷启动构建列表时，用户消息导航与列表元数据使用同一代历史。读取 V3 导航不再在投影重建途中迁移到 V4，避免发生“projection source identity changed”。正常压缩 V3 日志在列表读取前后保持原始字节不变；写入与继续执行仍由持久化层执行相邻版本迁移，并保留旧日志。无效或不支持的历史仍返回明确错误。

列表冷读取折叠标题、模型选择、列表状态、Token 用量和子任务计时，跳过不影响这些字段的流式 token，完整对话投影在打开任务时加载。列表缓存与完整投影分别校验，避免把轻量缓存误当作子智能体身份或完整历史；标题编辑的条件与列表字段使用同一序列水位。

## 上游条目与实现状态

| 能力 | Rust / Flutter 状态 | 后续验收或缺口 |
| --- | --- | --- |
| 自动审阅拒绝后人工决定 | 已接入新的人工作用域审批；不复用自动审批器或记忆授权；Never 与子代理限制保持有效 | 真实提供方、拒绝、同意、取消、断连组合验收 |
| 审阅故障独立提示 | `AUTO_REVIEW_FAILED` 与 `AUTO_REVIEW_DENIED` 分开 | 客户端全部错误样式统一 |
| 长对话无法继续发送 | 上下文超限识别覆盖 HTTP 413、结构化错误与流式错误，接入既有压缩恢复 | 多提供方真实长会话持续运行 |
| 过长工具输出字符损坏 | 既有输出保留器按 UTF-8 边界截断；溢出文件名补齐 UTF-16 代理对编码，避免 emoji 导致 panic | 大输出和连续调用实测 |
| 异常退出后的配置保存失败 | 文件锁改为操作系统锁，取消、panic、进程退出释放所有权；遗留 sidecar 不再永久阻断 | 升级时退出旧进程，避免不同锁协议混用 |
| 进行中的会话使用新工具 | 已有每步重新收集工具的机制及工具启停测试 | 各类外部插件逐项实测 |
| 每分钟重复提醒 | 旧会话提醒的创建、持久化解码与工具校验统一支持 60 秒；错过多次只处理最新一次 | rc.2 的 Host 任务库、六种规则、更新、全局恢复和发送记录尚未移植；旧运行时与会话回收存在冲突 |
| 时间上下文默认十分钟 | 已挂载生产 Host，默认 600000 ms；严格解码 JSON，保留显式 0 和自定义间隔；Web 插件设置支持读取、保存及冲突保护 | 原生 Flutter 配置入口与安装后操作验收 |
| 定时任务与时间上下文默认关闭 | 时间上下文默认关闭，支持手动启停；保存配置不会自动启用，更新配置会卸载旧监听器并重新加载 | 定时任务的默认关闭、Host 任务库与全局恢复尚未对齐 |
| 快捷键管理 | Flutter 支持查看、搜索、修改、恢复、全量冲突检查和侧栏当前键位提示 | macOS Command 映射和 Web 管理入口仍需补齐 |
| 窗口关闭后后台运行 | Flutter 与 Host 分离，关闭客户端不终止 Host | 应用退出影响提示、后台状态入口尚不完整 |
| 本地 Markdown 图片 | Flutter 已补盘符、中文空格、file URI 解析与点击缩放预览 | 原生安装版图像读取及错误状态验收 |
| 文件选择与系统关联 | 已有系统打开及工作台文件入口 | Windows 文件菜单图标、目录链接和跨平台关联验收 |
| 插件详情与配置恢复 | 已有插件库存、操作事务、恢复、取消；文件锁补强异常退出恢复 | 实际 GitHub 安装、镜像切换和失败页面闭环 |
| 账号模型与登录 | 账号登录、模型编辑、显示开关、推理参数已接入 | 账号/API Key 任务入口、退出确认与余额提示完整对齐未完成 |
| 桌面首次引导与 Platform 状态 | 未实现上游完整流程 | 额度介绍、用途选择、中断恢复及账号提示状态 |
| 归档三态筛选 | Web 与 Flutter 均支持隐藏已归档、全部、仅已归档及偏好保存；仅归档时隐藏空分组，支持直接打开和恢复归档历史 | 新客户端构建已生成；实际安装后的界面核对尚未执行 |
| 语音未就绪引导 | 已有语音按钮与状态处理 | 跳转插件设置及真实识别服务验收 |
| 视觉与交互优化 | 原生工具卡、代码高亮、工作台、文件差异已接入 | 全主题、窄窗口、键盘焦点、紧凑差异展示需统一检查 |

## Flutter 输入与设置

- Windows 支持 `Ctrl+V`、`Shift+Insert` 和输入框右键粘贴图片或复制的文件；PNG 保留透明度，截图位图转换为 PNG，资源管理器复制文件优先于附带缩略图。
- 图片与文件沿用现有附件上传协议、8 个附件和 16 MiB 总量限制；图片像素另有上限。图片显示缩略图，可移除后发送。
- 纯文本仍按原选择范围粘贴。附件读取期间禁止提前发送，切换会话后丢弃迟到导入结果，并发导入不能突破数量上限。
- macOS 与 Linux 的原生图片剪贴板尚未接入，文本粘贴继续使用平台默认行为。
- 技能与 MCP 管理支持编辑、删除、搜索、测试及错误回显；MCP 支持 stdio 与 HTTP、环境变量、请求头和工作目录。保存失败保留编辑草稿，未修改的字段保持原配置。
- 自动经验管理支持全局捕获开关、搜索和过滤、经验启停、建议确认、删除、诊断结论查看及当前任务候选预览；修改使用修订号保护，失败保留草稿，任务和 Host 切换取消旧请求。
- Flutter 团队任务派发／依赖／验收面板、插件安装卸载、技能导入与候选版本管理、诊断人工核查编辑仍未接入；Host 或 Web 已有接口不等于原生客户端已完成。

## 验收边界

后端上游适配相关 195 项、投影缓存 7 项、会话持久化 46 项、Dart SDK 64 项已通过；锁恢复、人工审批策略切换与 HTTP 错误分类 13 项 Release 专项通过，Host 自动审阅插件启停专项通过。2026-09-27 冻结 Flutter 源码全量测试 244 项通过、4 项条件跳过，静态分析无问题，Windows Release 构建完成。持久化有 3 项显式跳过，SDK 有 4 项需外部测试环境的测试未执行。Windows 剪贴板原生测试覆盖 WIC 编解码、颜色、透明度、大小限制与复制文件优先级。

时间上下文 crate 4 项测试、包含配置读写和 HTTP 错误契约的 API 网关全量 110 项测试，以及生产 Host 集成测试均通过。生产集成覆盖默认关闭、显式 0、自定义间隔、启停、配置重载、迟到回调抑制和现有 Agent 下一步注入。Web 配置卡、归档与侧栏回归通过，发布资源与源码摘要一致；这些结果对应移除独立任务验收功能之前的冻结版本，后续组合版本须重新执行相关回归。

2026-09-26 初次修复版的 Windows Host 与 Flutter Release 构建完成，本机两处 Host 和 Flutter 客户端共 197 个文件完成替换并核对摘要。安装时核对的 204 个会话日志及历史备份文件，以及设置、凭据、运行目录、工作区和客户端偏好，与安装前备份字节一致；原程序和数据备份保留。2026-09-26 12:07，已安装 Host 在原数据目录返回当时全部 131 个工作区会话，无遗漏，列表请求耗时 0.280 秒。此数量是该时间点的验证记录，后续正常删除不应由恢复流程撤销。后续 Flutter 构建、最终传输缓冲释放修复及时间上下文接入尚未安装。原生界面和真实剪贴板操作仍待确认，不能据此声明完整产品验收完成。

真实历史副本包含 131 个可列出的工作区会话，列表无遗漏；8 组历史样本覆盖长会话、工具结果、旧任务及子任务，并通过向前分页和冷重启检查。首次列表耗时 21.469 秒，重启后为 0.657 秒；列表读取未发布新的 V4 日志，源日志和副本中的旧代日志字节均未变化。

已安装构建 `5aebd007…` 的完整内存验收尚未达标。初步检查的冷列表峰值为 54,972,416 字节，历史读取／迁移阶段为 174,772,224 字节；扩大到 Web 每页 12 条、Dart 每页 80 条和 API 默认 100 条的 96 次请求后，全过程峰值为 281,288,704 字节，另有 4 次读取超出旧传输预算。现行 Host 全过程上限为 70,000,000 字节。历史传输及按需恢复改动须以重新冻结的候选构建验证，不能将源码修改或局部分配量测试视作安装版性能达标。

普通 Release 候选 `254177c712e01006817d665b641899c5c5b666855986cebeb45894ef3a47df5c` 的读取阶段已完成 96 次请求和 12 轮双向完整遍历：92 个原成功响应的内容摘要一致，4 个原超预算请求均成功，130 个有效工作区会话无遗漏或额外恢复，源日志字节未变。全过程峰值为 77,246,464 字节，仍超过上限，因此该候选未通过整体验收、未安装。旧会话继续执行、取消与冷重启以及进一步峰值优化需单独验证。

后续普通 Release 候选 `f621426131a3258e28b82dfd7fc3ab5cdeb9e5fd8267e56d4a7e41334d54e8c5` 通过 96 次历史请求、12 轮双向完整遍历和 130 个有效会话清单比对，源日志字节未变。三个旧会话的继续执行、取消和冷重启共 9 个阶段通过，9 条用户消息均仅写入一次，6 次完整回复和 3 次取消状态正确；历史前缀与 22 个引用附件保持一致。历史读取全过程峰值为 83,689,472 字节，旧会话执行全过程峰值为 77,639,680 字节，均未满足 70,000,000 字节上限。冷重启阶段的较低峰值不能代替全过程结果。该候选未安装，且早于最终传输缓冲释放修复和时间上下文接入；后两项已有专项测试，尚无包含全部改动的普通 Release 完整负载结果。

单元测试、组件测试和隔离 Host 检查分别覆盖对应代码路径。外部账号、真实语音服务、全局提醒恢复、跨平台安装、自动更新与完整内存负载属于独立验收项。完整平台状态见[桌面平台支持与验收](desktop-platforms.zh.md)，开发计划状态见[更新计划](plans/更新计划.md)。
