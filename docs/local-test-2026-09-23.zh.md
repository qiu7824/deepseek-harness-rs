# 本地实测清单

核对日期：2026-09-24。  
已安装版本：`0.1.3-alpha.34+local.20260923.3`。
安装验证：启动、发送消息、重启续聊及原有会话读取通过。  
安装目录：`D:\Program Files (x86)\DeepSeek Harness-rs\core`  
验收性质：本地实测版，未完成整体验收或公开发布。

安装时核验了 252 个部署文件，129 个原有会话全部保留，用户设置内容未变；原程序文件和应用数据已有备份。重启后账本仍为 120 条诊断、10 条经验，已删除的读取诊断没有重新出现。已有浏览器页面需刷新后使用新前端。

## 版本与验证范围

| 对象 | 当前状态 | 识别依据 |
| --- | --- | --- |
| 本机安装版 | `0.1.3-alpha.34+local.20260923.3`；构建用时 232.3 秒，启动器和控制组件配套更新 | Host SHA-256 前缀 `0e28c611fca6`；[本轮安装与实测证据](plans/local-install-2026-09-23-local3.json) |
| 旧安装基线 | `.1` 已替换，保留文件和数据备份 | Host SHA-256 前缀 `1cd2e3cacc2a7`；[历史安装证据](plans/local-install-2026-09-23.json) |
| 中间候选 | `.2` 及早期 `.3` 仅保留为定位、对照依据 | 同版本中间候选须以摘要区分，不能替代当前安装程序的验收 |

“专项通过”只覆盖所列场景；“候选通过”不等于“已安装”。隔离 Host 测试使用本地确定性模型，不代表真实语音服务或所有外部模型均已验收。

相关回归记录包含目标 11 项、命令取消 2 项、Host 287 项、API 62 项及压缩包 3 项；取消优先级用例已由失败转为通过。Host 回归仍有 4 项实机检查未运行，另有 1 个内部图像子进程入口按默认设置忽略。各项结果只证明其对应检查范围。

## 已发现问题与验收状态

| 问题 | 安装版或候选的实测结果 | 修复与复验状态 |
| --- | --- | --- |
| 子任务执行中改写目标 | 旧 `.1` 可在子任务仍为 running 时把目标 A 改成 B | `.3` 已安装修复，同摘要程序已复验拒绝目标修改、清除及任务要求编辑 |
| 手动压缩不能随停止结束 | 旧候选既有后端取消缺口，也有前端缺少停止按钮的问题 | `.3` 已通过实际按钮、第二窗口、刷新恢复及状态同步；停止后不发布迟到摘要 |
| 取消被误报为摘要失败 | 提供方先返回取消错误时，旧代码误判为摘要质量问题 | `.3` 已修复取消优先级，实际界面显示“压缩已停止” |
| 采用当前目标后无法保存 | 旧绑定状态持续禁用保存按钮 | 修复已安装；已有浏览器专项验证采用目标、恢复草稿、保存后续任务及重新验收，全部编辑场景仍待验收 |
| 任务面板窄屏溢出 | 长任务标题使 342 px 内容区出现 450 px 滚动宽度 | 修复已安装；390 px 专项两项宽度均为 342 px，暗色主题仍需补验 |
| Host 内存超标 | 旧 `.1` 的峰值为 588,771,328 字节，超过 70 MB 上限 | `.3` 已包含有界历史重建；新版完整负载峰值尚未验收 |

目标修改与手动压缩的证据见 [执行边界与命令取消记录](plans/goal-command-control-2026-09-23.json)，编辑和排版证据见 [实际浏览器验证](plans/task-edit-browser-2026-09-23.json)。

## 已接入的功能与修复

下表的“专项通过”仅覆盖已有测试场景，最终结果以当前安装版本的实际使用为准。

| 检查项 | 已接入内容 | 实测操作与预期 | 当前证据 |
| --- | --- | --- | --- |
| DOCX 预览 | 侧栏使用 docx-preview，支持分页、缩放和适合宽度；源码已将消息文件弹窗接入同一组件 | 打开含中文表格的多页 DOCX，查看翻页、缩放、关闭重开和窄窗口布局 | 侧栏浏览器专项已通过；消息弹窗的路由、翻页、打印切换和关闭清理检查通过，仍需新安装版实际文档复验 |
| WPS 打印版式与 PDF | 显式使用 WPS 导出 PDF，支持预览和翻页 | 在 DOCX 查看器切换打印版式；检查文字、表线、分页及 PDF 适合宽度 | 原失败文档及多页样本专项通过 |
| 文件附件与只读输入 | 源码已接入原始字节存储、结构化引用、模型句柄、会话授权及 Web/Flutter 文件卡片 | 上传后发送、预览、渲染和重开；其他会话不得读取，原件不被编辑器写回 | 存储、RPC→模型→沙箱及客户端专项通过；尚未安装，实际文档和冷恢复仍待集中验收；见 [附件与预览证据](plans/file-attachment-preview-integration-2026-09-24.json) |
| 响应计时 | 累计首字前等待、失败与取消耗时，旧缓存重算 | 发起请求后停止，再发送一轮；核对累计时间，重启后不应回到错误的旧值 | 正常、取消、失败、恢复及重启专项通过 |
| 文件读取参数 | 接受合法的整值浮点参数，如 1.0；仍拒绝非法范围 | 重试曾出现 offset 错误的读取任务 | 一条诊断涉及的 8 次原调用已用安装版同摘要程序重放通过，原文件不变；其他读取记录仍待逐案复验 |
| 工具合同参数 | 明确要求 contract 为对象，阻止字符串化合同 | 重试原合同任务；非法参数应有明确错误，不能掩盖已有执行效果 | 合同专项通过 |
| 停止与迟到结果 | 取消模型请求与命令，阻止旧子任务结果复活已停止父任务 | 模型等待时停止、压缩时停止、父任务重复停止、再发送新消息和新委派，随后重启 | 父子任务及压缩组合通过；实际压缩停止按钮、跨窗口与刷新恢复通过，仍非全部停止场景验收 |
| 上下文压缩显示 | 修正 token 扣减和缓存重算，显示压缩开始、结束及失败 | 触发压缩后查看上下文用量与状态，再重启核对 | token 与压缩生命周期专项通过 |
| 图片放大 | 工具图片接入图片查看器，脱离聊天容器裁切 | 点击图片应在应用内放大，关闭后回到原位置 | 桌面、明暗主题及窄屏组件专项通过；窄屏原尺寸缩放仍未完成 |
| 语音输入界面 | 输入框长按、过程中写入中间文字、松开结束、取消保护；按钮顺序为模型→语音→发送 | 使用真实麦克风长按输入框，检查首字延迟、持续更新、松开、拖动取消与切换会话 | 组件和插件联合测试通过；真实识别速度与效果未完成验收 |
| Computer Use 侧栏 | 控制操作发布所属会话活动，触发侧栏附着 | 发起电脑控制，检查是否自动打开对应侧栏及光标显示 | 控制包和侧栏专项通过；真实桌面、DPI 与远程场景待验收 |
| 完全访问策略 | 安全盾默认跟随可信会话访问模式，保留显式规则与后续合同检查 | 在完全访问模式复测原跨工作区普通操作 | 策略及设置专项通过；原安装版故障待复验 |
| 工作区环境设置 | 保留未修改配置、处理保存冲突和丢失回执；区分 Python 自动、项目环境和解释器 | 修改一项环境设置后保存、重开，检查其他配置是否保留 | 设置专项通过；远端执行能力尚未完整实现 |
| 会话写入占用 | 会话创建和恢复取得跨进程写入权；普通读取不占写入权；退出后释放 | 正常重开旧对话；重复打开不应损坏历史或误报为不存在 | 持久化互斥与实际 Agent 恢复专项通过 |
| 用户终端 | 源码已采用独立用户会话登记，停止或退休 Agent 不再作为用户终端的生命周期边界 | 无运行中 Agent 时打开、输入、切换和关闭；模型终端继续执行其沙箱策略 | 真实 PTY、跨会话拒绝、取消后迟到启动及退出清理通过；尚未安装 |
| 插件操作与配置恢复 | 源码已接通包/配置回滚、已验证快照、安装和移除取消、提交回执；可选客户端不阻断核心界面启动 | 安装、取消、移除、重启、损坏配置恢复及失败插件隔离 | Host 292 项回归通过；真实 CLI 卸载/恢复、进程中断恢复及界面专项通过；启停取消、回滚与持久日志专项已通过，安装版复验未完成；见 [证据](plans/user-terminal-plugin-recovery-2026-09-24.json) |
| 协作入口 | 协作入口位于右上角，子任务可进入完整对话 | 从协作目录打开子任务、返回父任务，检查停止、结果和任务状态 | 一步进入子对话、父子路径及迟到回执隔离的组件回归通过；完整安装版人工流程待验收 |
| 目标与任务验收绑定 | 修改目标后，旧目标验收不再满足新目标；明确采用当前目标后可保存 | 修改目标后检查旧任务的过期状态，新要求必须重新验收 | 修复已安装，绑定与重新验收的专项通过；其余编辑冲突和界面场景待验收 |

任务要求的实际浏览器保存、草稿恢复和重新验收链路已有专项证据，长标题窄屏溢出及英文验收提示的修复现已安装。该历史浏览器专项使用旧 Host 配合候选前端，新安装版仍需完成全部界面矩阵。见 [任务编辑浏览器实测](plans/task-edit-browser-2026-09-23.json)。

安装版同摘要程序的隔离复验中，多次工具调用、下一轮输入、冷重启的上下文快照数量保持 3/3/3；首轮后两次快照变化仅来自任务建立及任务状态变化。父子任务组合保持 1 次稳定快照，停止后的迟到结果没有新增父回合或模型请求。该结果不代表压缩、环境变化和任务要求变更叠加场景全部通过。见 [上下文与停止实测](plans/context-stop-restart-2026-09-23.json)。

`.3` 的同摘要程序已通过六组实际 Host 组合：运行中子任务阻止要求修改、停止压缩后拒绝迟到摘要、子任务执行期间完成长历史压缩、压缩后修改并采用新要求、冷重启保持目标及约束，以及自动压力压缩的完整结束事件。此版本现已安装，结果仍属于专项验收。[安装及组合证据](plans/local-install-2026-09-23-local3.json)。

## 自动经验与诊断

最新复核后账本为 **117 条诊断、10 条经验，共 127 条记录**；记录数不等于独立软件缺陷数。[根因与证据矩阵](plans/diagnostic-repair-batches-2026-09-24.zh.md) 仅用于去重和追溯；实施范围包含全部未完成项和所有已有修复的集成，不以单条记录、分组或某个候选版本划定边界，完成后集中验收。

- 2026-09-24 使用安装版同摘要程序重放 4 次原始只读命令，全部成功；已删除对应 3 条沙箱初始化诊断，无关记录未变。[原案关闭证据](plans/native-diagnostics-closure-2026-09-24.json)。
- 其余 24 条此前无核对标记的记录已补充分类；归因标签不等于修复完成，现存记录中 30 条证据不足，另有 2 条历史归因待更新。[归因记录](plans/diagnostic-original-review-2026-09-24.json)。
- 原会话产物漏显已定位到缺少回合起点的历史窗口；附件移除控件已统一并锁定发送状态，候选前端通过原会话和明暗、窄窗口验证，尚未替换安装。[候选前端证据](plans/artifact-attachment-acceptance-2026-09-24.json)。

- 有效整数读取的 8 次原调用、新工作区的一次 Shell 原失败，均已复验并删除对应诊断；删除保留证据并校验记录版本。[读取闭环](plans/read-diagnostic-closure-2026-09-23.json)、[沙箱原案闭环](plans/shell-diagnostic-closure-2026-09-23.json)。
- 另有 29 次读取原调用已用安装版同摘要程序逐次重放通过，原文件保持不变，已删除对应 3 条诊断；无关记录未变。剩余 27 条诊断尚无归因复核标记。[批量读取闭环](plans/read-diagnostics-batch-closure-2026-09-23.json)。
- `D:\游戏` 的原未初始化环境已经恢复，通用首次使用自动初始化代码现已安装；其他工作区、槽位占用与恢复场景仍须验收。[首次初始化证据](plans/native-first-use-2026-09-23.json)。

## 内存验收

Host 主进程上限为 **70,000,000 字节**，覆盖启动、任务执行、取消恢复与任务结束全过程。空闲值、小规模专项或辅助进程转移不能替代完整负载验收；辅助进程另行记账。

| 隔离诊断场景 | Host 峰值 | 首次列表耗时 |
| --- | ---: | ---: |
| 空数据目录 | 38.15 MB | 0.062 秒 |
| 原有数据首次打开，显示 129 个会话 | 463.00 MB | 26.000 秒 |
| 缓存生成后重启，仍显示 129 个会话 | 57.53 MB | 0.359 秒 |
| 仅恢复一个约 14.4 万事件会话的旧缓存 | 450.85 MB | 2.859 秒 |

这些数据来自安装版同摘要程序，证实整段载入和复制旧历史是一个超标来源；不能据此认定全部内存来源或泄漏已排除，也不能作为新候选达标证据。[真实历史定位](plans/real-history-memory-diagnosis-2026-09-23.json)。

## 尚未完成，不能计为已修复

- 自动经验全部剩余记录的共同根因修复、证据映射与批量复验删除；缺失原始证据及未知副作用的记录保留。
- Session V4 的新安装版联合验收。修复源码已默认写入 V4，接通迁移、残尾恢复、继承边界和 CLI 导入；尚未替换安装版或完成原有会话的集中实测。[运行时接入证据](plans/native-runtime-integration-2026-09-24.json)。
- 独立账户沙箱首次初始化的安装版验收，以及启动失败、槽位占用和隔离恢复的完整场景。
- 真实语音服务的持续出字效果、网络异常与延迟验收。
- Goal、Todo、标题及任务要求编辑的全部绑定、冲突、重启恢复与验收失效规则；更多真实任务中的停止与恢复组合。
- 长会话、环境变化、协同和重启组合下的上下文注入完整检查。
- 原“插件设置后工作区消失”的确切触发链、插件启停完整取消与操作记录及所有产物识别问题；用户终端和插件恢复源码已接通，仍需安装版联合验收。
- GitHub Actions、SSH 远端构建/执行和批量高负载绑定的完整闭环。
- 归档对话垃圾槽、日志与编译缓存清理的完整操作和恢复验证。
- 全 UI 的统一排版、附件删除控件、所有主题、窄窗口和错误状态检查。
- Host 主进程内存优化与全输入规模验收：旧安装版已确认超标，新版完整负载尚未验收；集中实际使用验收尚未完成。
- 最终安装包、便携包、公开版本推送、蓝奏上传和回下载校验。

## 问题记录

记录失败的操作步骤、所在页面或会话、预期结果与实际结果；同一问题补充新的触发条件。涉及重启、压缩或停止时，保留发生顺序。

| 检查项 | 实际结果 | 触发步骤或问题 |
| --- | --- | --- |
| 文档预览与打印版式 | 待实测 | |
| 响应计时与等待模型 | 待实测 | |
| 停止、恢复与任务编辑 | 修复已安装，部分专项通过 | 子任务执行边界、目标绑定及压缩停止已验证；全部真实任务和编辑冲突仍待验收 |
| 上下文压缩与注入 | 六组组合及实际停止按钮通过 | 跨窗口与刷新恢复通过；其他 Provider、更多长会话与环境变化待验收 |
| 协作入口与子任务对话 | 待实测 | |
| 语音输入 | 待实测 | |
| 图片、附件与全 UI | 部分专项通过 | 长标题窄屏修复已安装；附件删除、暗色主题及其余页面待验收 |
| 沙箱、权限与环境设置 | 部分专项通过 | 原新工作区已恢复，通用自动初始化与完全访问原案仍需安装版验收 |
| 原失败任务与产物显示 | 待实测 | |
